// const icons = document.querySelectorAll(".tech");

// setInterval(() => {
//   icons.forEach(icon => {
//     icon.style.transform = `scale(${1 + Math.random() * 0.4})`;
//     setTimeout(() => {
//       icon.style.transform = "scale(1)";
//     }, 300);
//   });
// }, 2800);
